# Core numerics — the index implementations depend only on numpy.
numpy>=1.24

# Data loading / benchmarking tables
pandas>=2.0

# Semantic app. The mood translator is a self-contained rule-based lexicon —
# no LLM/API dependency, so it runs fully offline.
streamlit>=1.30

# C++ hot-path build (optional stretch goal). Only needed to compile cpp/.
pybind11>=2.11

# Tests
pytest>=7.4

# Benchmark plots (benchmarks/plot_results.py)
matplotlib>=3.7
